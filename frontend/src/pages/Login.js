import LoginComponent from '../components/LoginComponent';  // Adjust the path accordingly

const Login = () => {
  return (
    <div className="login-page">
      <LoginComponent />
    </div>
  );
};

export default Login;